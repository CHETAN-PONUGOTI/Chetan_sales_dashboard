{
  "name": "sales-analytics-dashboard",
  "version": "1.0.0",
  "description": "A MERN stack sales analytics dashboard.",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run server --prefix backend\" \"npm start --prefix frontend\"",
    "install-deps": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "seed": "npm run seed --prefix backend"
  },
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}